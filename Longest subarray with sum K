class Solution:
    def longestSubarray(self, nums, k):
        longest=0
        
        n=len(nums)
        for i in range(n):
            arrsum=0
            for j in range(i,n):
                arrsum+=nums[j]
                if arrsum==k:
                    longest=max(longest,j-i+1)
        return longest
