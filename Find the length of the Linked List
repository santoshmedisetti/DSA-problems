class Solution:
    def getLength(self, head):
        # Your code goes here
        count=0
        current=head
        while current:
            count+=1
            current=current.next
        return count
