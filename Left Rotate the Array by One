def left_rotate_by_one(arr):
    if len(arr) == 0:
        return arr
    
    first = arr[0]
    for i in range(len(arr) - 1):
        arr[i] = arr[i + 1]
    arr[-1] = first
    
    return arr

# Example
arr = [1, 2, 3, 4, 5]
print(left_rotate_by_one(arr))
