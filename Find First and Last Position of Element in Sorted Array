class Solution:
    def searchRange(self, nums: List[int], target: int) -> List[int]:
        for i in range(len(nums)):
            if nums[i]==target:
                fast=i
                fas=fast
                if fast!=-1:
                    last=i
        return [fas,last]
        
