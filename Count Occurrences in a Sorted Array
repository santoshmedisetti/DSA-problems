class Solution:
    def countOccurrences(self, arr, target):
        # Your code goes here
        count=0
        for i in range(len(arr)):
            if arr[i]==target:
                count+=1
        return count
